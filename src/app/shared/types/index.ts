export interface Produto {
  id: number;
  nome: string;
  categoria: string;
  estoque: number;
  unidade: string;
  valorUn: string;
  emoji: string;
}

//export type Categorias = "Paes" | "Salgados" | "Doces" | "Bebidas";
