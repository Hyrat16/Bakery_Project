import { type Produto } from "../../shared/types";
import { useState } from "react";

export const buscaProdutoAtivo = (value: string) => {
  if (value.length != 0) {
    console.log("Bateu");
  }
};

